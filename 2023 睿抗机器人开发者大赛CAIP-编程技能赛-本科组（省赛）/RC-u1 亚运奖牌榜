/*
//输入格式:
输入第一行是一个正整数 N (1≤N≤1000)，表示总共有 N 条获奖记录。
接下来的每一行都是形如以下的一条记录：
Ci,Pi
其中 Ci=0,1，0表示是第一个国家/地区，1表示是第二个国家/地区；
Pi=1,2,3，1 表示金牌，2 表示银牌，3 表示铜牌。
//输出格式:
首先输出两行，第一行是第一个国家/地区的金牌、银牌、铜牌获得数，用空格隔开；第二行是第二个国家/地区的奖牌获奖情况，要求与格式同第一个国家/地区。
最后一行，如果是第一个国家/地区排在前面，输出 The first win!，否则输出 The second win!。
排在前面的定义是：先比较金牌数，金牌数较大的排在前面；如金牌数相等，比较银牌数，银牌数较大的在前面；如金牌银牌数都相等，则比较铜牌数，铜牌数较大的在前面。
保证数据不存在奖牌数完全相同的情况。*/
```
#include <iostream>
#include<math.h>
using namespace std;
int main()
{
    int n;
    scanf("%d", &n);
    int guo, pai, jin1=0, yin1=0,tong1=0, jin2=0,yin2=0,tong2=0;
    for(int i=0;i<n;i++){
        scanf("%d %d", &guo, &pai);
        if(guo ==0){
            if(pai==1)
                jin1++;
            else if(pai==2)
                yin1++;
            else if(pai==3)
                tong1++;
        }
        else{
            if(pai==1)
                jin2++;
            else if(pai==2)
                yin2++;
            else if(pai==3)
                tong2++;
        }
    }
    int big=0;
    if(jin1>jin2)
        big=1;
    else if(jin1<jin2)
        big=2;
    else{
        if(yin1>yin2)
            big=1;
        else if(yin1<yin2)
            big=2;
        else{
            if(tong1>tong2)
                big=1;
            else if(tong1<tong2)
                big=2;
            
        }
    }
    cout<<jin1<<" "<<yin1<<" "<<tong1<<endl;
    cout<<jin2<<" "<<yin2<<" "<<tong2<<endl;
    if(big==1)
    cout<<"The first win!"<<endl;
    else if(big==2)
        cout<<"The second win!"<<endl;

    return 0;
}
```cpp
